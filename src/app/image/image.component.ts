import { Component, Inject, OnInit } from '@angular/core';

export interface ImageComponent {

     id?: string,
     url?: string,
     name?:string,   
     description?: string,
     blur?: string
}
